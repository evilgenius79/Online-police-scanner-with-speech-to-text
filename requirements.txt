# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.1
jinja2>=3.1.2

# Audio capture
sounddevice>=0.4.6
numpy>=1.24.0

# Voice Activity Detection
# webrtcvad-wheels provides pre-built wheels for Python 3.10/3.11/3.12
# If this fails to install, try: pip install webrtcvad
webrtcvad-wheels>=2.0.14

# Speech-to-text (GPU-accelerated Whisper)
# Requires CUDA 11.x or 12.x + cuDNN 8.x or 9.x
# See setup.sh for full installation instructions
faster-whisper>=1.0.0
